Delivery Hero 财报看板本地预览

请不要直接双击 index.html（file:// 可能导致浏览器限制本地资源）。

Windows：双击 start_preview.bat，然后在浏览器打开 http://localhost:4173
macOS / Linux：在本目录运行 node server.cjs，然后打开 http://localhost:4173

任意路径会由本地服务返回 index.html，以支持 SPA fallback。
